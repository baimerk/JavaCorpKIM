package lambda_one;

public interface ElectricityConsumer {
    // состоят из наборов заголовков методов
    // функциональные интерфейсы - содержат только один абстрактный метод
    void electricityOn(Object sender);
}
